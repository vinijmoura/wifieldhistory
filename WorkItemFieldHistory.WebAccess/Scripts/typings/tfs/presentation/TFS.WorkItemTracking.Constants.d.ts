
declare module "Presentation/Scripts/TFS/Generated/TFS.WorkItemTracking.Constants" {
    enum FieldType { Html, DateTime }
    enum CoreField {
        Id,
        History,
        ChangedDate,
        RevisedDate,
        ChangedBy,
        AuthorizedAs,
        AuthorizedDate,
        Watermark,
        RelatedLinkCount,
        HyperLinkCount,
        ExternalLinkCount,
        AttachedFileCount
    }
    enum DalFields {
        PersonID
    }
}
